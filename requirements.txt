# One known-good point inside the ranges in pyproject.toml: the versions this
# repo was developed and evaluated against (Python 3.13, macOS, Sep 2026).
# Equivalent to `pip install -e ".[dev]"` with everything frozen.
#
#   pip install -r requirements.txt

# Editable, so experiment configs under configs/ are picked up from the checkout.
-e .

# runtime
gymnasium==1.3.0
stable-baselines3==2.9.0
numpy==2.5.3
pandas==3.0.6
pyyaml==6.0.3
torch==2.14.0
scikit-learn==1.9.1
joblib==1.6.0

# dev extra: tests, tuning, figures
pytest==9.1.1
optuna==5.0.0
matplotlib==3.11.2
