# Example: default tree demand + PPO (explicit single-file experiment)
demand:
  kind: tree_elastic
  elasticity: -1.2
  ref_price: 100.0

algorithm:
  name: ppo
  ent_coef: 0.02
  n_envs: 2

train:
  total_timesteps: 50000
  seed: 42
